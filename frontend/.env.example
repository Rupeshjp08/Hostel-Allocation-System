# ── Frontend environment variables ────────────────────────────────────────────
# Used only for LOCAL development. On Vercel production, do NOT set VITE_API_URL;
# the app falls back to "/api" which routes to the same-origin serverless function.

VITE_API_URL=http://localhost:5000/api
